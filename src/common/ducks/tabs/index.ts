import reducer, { tabsInitialState } from "./reducers";
import * as tabsOperations from "./actions";
import * as tabsTypes from "./types";

export { tabsOperations, tabsInitialState, tabsTypes };

export default reducer;
