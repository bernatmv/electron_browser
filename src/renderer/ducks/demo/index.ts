import reducer, { demoInitialState } from "./reducers";
import * as demoOperations from "./actions";
import * as demoTypes from "./types";

export { demoOperations, demoInitialState, demoTypes };

export default reducer;
